<!doctype html>
<html>
include head.pug
<body>
include header.pug
div(class="uk-child-width-expand@s uk-text-center uk-margin-top" uk-grid)
    div(class="uk-width-1-3@xl uk-width-1-4@l uk-width-1-6@m")
    div(class="uk-width-expand")
        div(class="uk-card uk-card-large uk-card-default")
            div(class="uk-card-header")
                if user
                    h2(class="uk-card-title")= `Hey ${user.nickname}!`
                else
                    h2(class="uk-card-title")= `Welcome to Seen 👀!`
            div(class="uk-card-body")
                if user    
                    h4
                        | Use Seen 👀 to know when your email is read or when a page in your
                        | website is seen. Just create a pixel and paste it in the email
                        | you want to be tracked. It is absolutely invisible and will have
                        | zero visual effects on your email content.
                else
                    h4
                        | Seen 👀 is a tracking service for emails and websites.
                        | Create an invisible tracking pixel and paste it in your
                        | email to see when someone opens your email. Join now
                        | to create your first pixel, no credit card required!
            div(class="uk-card-footer")
                if user    
                    a(class="uk-button uk-button-default uk-margin-small-right" type="button" href="/create") New pixel
                    a(class="uk-button uk-button-default uk-margin-small-right" type="button" href="/track") Track a pixel
                else
                    a(class="uk-button uk-button-default uk-margin-small-right" type="button" href="/create") Join
    div(class="uk-width-1-3@xl uk-width-1-4@l uk-width-1-6@m")
</body>
include libs.pug
</html>
